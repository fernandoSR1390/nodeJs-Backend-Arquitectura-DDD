const express = require('express');
/* const { jwtMiddleware } = require('../middleware/jwtMiddleware');
const { postAllPatients } = require('../controllers/hospital/hospitalController');
 */
const router = express.Router();

/* router.get('/job-list', jwtMiddleware, userController.getJoblist);
router.get('/current-user', jwtMiddleware, userController.getCurrentUser);

router.get('/prueba', userController.pruebaPostgreSQL); */

/* router.post('/patients', postAllPatients) */

module.exports = router